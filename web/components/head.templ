package components

import (
	"github.com/hookenz/app-template/web/deps"
)

templ Head(siteTitle string) {
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		@deps.Bootstrap()
		<title>{siteTitle}</title>
	</head>
}
